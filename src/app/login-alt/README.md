# Notes
> This directory is made because I was being redirected by the proxy from accessing `/login`
and was redirected to `/dashboard`. After resolving those, I will migrate this route into `(auth)/login`